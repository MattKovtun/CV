pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html favicon.svg public/
    - cp -r fonts css js public/
    - cp *.pdf public/ 2>/dev/null || true
  artifacts:
    paths:
      - public
  only:
    - main
